<?xml version="1.0" encoding="utf-8"?>
<local:ModuleViewBase
    x:Class="AppHost2.Pages.ActivationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AppHost2.Pages"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Windows Activation" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Kiểm tra trạng thái kích hoạt, dò dấu vết KMS, kích hoạt bằng MAS." Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7"/>
        </StackPanel>

        <!-- Trạng thái kích hoạt -->
        <Border Grid.Row="1" Padding="20,14" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="6">
                    <ProgressRing x:Name="StatusRing" IsActive="False" Width="28" Height="28"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="6">
                    <TextBlock x:Name="ActivationState" Text="Đang kiểm tra..." FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="EditionText" Text="" Opacity="0.7" FontFamily="Consolas"/>
                    <TextBlock x:Name="PartialKey" Text="" Opacity="0.5" Style="{ThemeResource CaptionTextBlockStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Dấu vết KMS -->
        <Border Grid.Row="2" Padding="20,14" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
                    <TextBlock Text="DẤU VẾT KMS" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button x:Name="ScanTracesBtn" Content="🔍 Dò dấu vết" Click="ScanTracesBtn_Click" Padding="12,4"/>
                    <Button x:Name="CleanKmsBtn" Content="🧹 Dọn KMS (dry-run)" Click="CleanKmsBtn_Click" Padding="12,4"/>
                    <ProgressRing x:Name="TraceRing" IsActive="False" Width="20" Height="20"/>
                </StackPanel>
                <TextBlock Grid.Row="1" x:Name="TracesResult" Text="(bấm Dò dấu vết để kiểm tra)" Opacity="0.6" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"/>
            </Grid>
        </Border>

        <!-- Phương thức kích hoạt -->
        <Grid Grid.Row="3" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="PHƯƠNG THỨC KÍCH HOẠT (MAS)" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" FontWeight="SemiBold"/>
            <ListView Grid.Row="1" x:Name="MethodsList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,8" ColumnSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0" Glyph="{Binding Icon}" FontSize="20"/>
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Description}" Opacity="0.55" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Button Grid.Column="2" Content="Kích hoạt" Click="ActivateBtn_Click" Tag="{Binding Name}" Padding="16,6" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</local:ModuleViewBase>