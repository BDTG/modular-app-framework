<?xml version="1.0" encoding="utf-8"?>
<local:ModuleViewBase
    x:Class="AppHost2.Pages.TweaksView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AppHost2.Pages"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="System Tweaks" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="DescText" Text="Tinh chỉnh registry (network + system) — toggle từng mục, rollback khi tắt." Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <ProgressRing x:Name="LoadRing" IsActive="False" Width="22" Height="22"/>
            <Button x:Name="ReloadBtn" Content="⟳ Tải lại" Click="ReloadBtn_Click"/>
            <Button x:Name="RollbackAllBtn" Content="↩ Khôi phục tất cả" Click="RollbackAllBtn_Click"/>
            <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
        </StackPanel>

        <ListView Grid.Row="2" x:Name="TweaksList" SelectionMode="None" IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,10" ColumnSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="3">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding Description}" Opacity="0.55" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
                        </StackPanel>
                        <InfoBadge Grid.Column="1" VerticalAlignment="Center" Value="{Binding GroupLabel}" Style="{ThemeResource AttentionIconInfoBadgeStyle}"/>
                        <ToggleSwitch Grid.Column="2" IsOn="{Binding Applied, Mode=TwoWay}" Toggled="ItemToggle_Toggled" Tag="{Binding Index}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</local:ModuleViewBase>