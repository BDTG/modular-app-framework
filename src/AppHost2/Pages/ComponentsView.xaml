<?xml version="1.0" encoding="utf-8"?>
<local:ModuleViewBase
    x:Class="AppHost2.Pages.ComponentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AppHost2.Pages"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="🔧 Components Remover" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Gỡ Edge/OneDrive/Defender/UpdateHealth/PCHealth — 5 scripts." Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <ProgressRing x:Name="LoadRing" IsActive="False" Width="22" Height="22"/>
            <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center"/>
        </StackPanel>
        <ScrollViewer Grid.Row="2">
            <ListView x:Name="ItemsList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,10" ColumnSpacing="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="2">
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Description}" Opacity="0.55" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Thực hiện" Click="Action_Click" Tag="{Binding Key}" Padding="14,6" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</local:ModuleViewBase>