<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppHost2.Pages.ModulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Module Manager" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Cài module từ file zip, bật/tắt, gỡ — như KernelSU Manager." Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.7"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button x:Name="InstallBtn" Content="📦 Install từ file..." Click="InstallBtn_Click" Style="{ThemeResource AccentButtonStyle}" Padding="16,8"/>
            <Button x:Name="StartAllBtn" Content="▶ Tất cả" Click="StartAllBtn_Click" Padding="16,8"/>
            <Button x:Name="StopAllBtn" Content="■ Tất cả" Click="StopAllBtn_Click" Padding="16,8"/>
            <Button x:Name="RefreshBtn" Content="⟳ Quét lại" Click="RefreshBtn_Click" Padding="16,8"/>
            <ProgressRing x:Name="WorkRing" IsActive="False" Width="22" Height="22"/>
            <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <ListView x:Name="ModulesList" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Card_Click">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="16,12" CornerRadius="8" Margin="0,4"
                                Background="{ThemeResource CardBackgroundFillColorDefault}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <Grid ColumnSpacing="14" RowSpacing="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Hàng 1: icon + tên + version + toggle -->
                                <FontIcon Grid.Row="0" Grid.Column="0" Glyph="{Binding Icon}" FontSize="24" VerticalAlignment="Center"/>
                                <StackPanel Grid.Row="0" Grid.Column="1" Spacing="2">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"/>
                                        <TextBlock Text="{Binding Version}" Opacity="0.5" FontSize="12" VerticalAlignment="Center" FontFamily="Consolas"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Id}" Opacity="0.55" FontSize="12" FontFamily="Consolas" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <ToggleSwitch Grid.Row="0" Grid.Column="2" IsOn="{Binding Enabled, Mode=TwoWay}" Toggled="EnableToggle_Toggled" Tag="{Binding Id}" VerticalAlignment="Center" OffContent="Tắt" OnContent="Bật"/>

                                <!-- Hàng 2: trạng thái -->
                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Spacing="12">
                                    <TextBlock Text="{Binding StateText}" Foreground="{Binding StateBrush}" FontSize="12.5" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding PidText}" Opacity="0.5" Style="{ThemeResource CaptionTextBlockStyle}" FontFamily="Consolas"/>
                                    <TextBlock Text="{Binding RestartText}" Opacity="0.5" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                </StackPanel>

                                <!-- Hàng 3: hành động -->
                                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="0,2,0,0">
                                    <Button Content="▶ Start" Click="StartBtn_Click" Tag="{Binding Id}" Padding="14,5" FontSize="12"/>
                                    <Button Content="■ Stop" Click="StopBtn_Click" Tag="{Binding Id}" Padding="14,5" FontSize="12"/>
                                    <Button Content="📁 Mở" Click="OpenBtn_Click" Tag="{Binding Id}" Padding="14,5" FontSize="12"/>
                                    <Button Content="🗑 Gỡ" Click="RemoveBtn_Click" Tag="{Binding Id}" Padding="14,5" FontSize="12" Foreground="#FF6B6B"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
    </Grid>
</Page>